#include <iostream>
#include<algorithm>
using namespace std;
int kthelement(int arr[],int n,int k)
{
  sort(arr,arr+n);


  return arr[k-1];  
}
int main()
{
  int n,k;
  cin>>n;
  cin>>k;
  int arr[n];a
  for(int i=0;i<n;i++)
  {
    cin>>arr[i];
  }

  cout<<kthelement(arr,n,k);
  




  return 0;
}
