#include <iostream>
using namespace std;
int linear(int arr[],int n)
{
  for(int i=0;i<n;i++)
  {
    if(arr[i]==i)
    {
      return i;
    }    
  }
  return -1;
}
int main()
{
  int arr[]={-10, -5, 0, 3, 7};
  int n=sizeof(arr)/sizeof(arr[0]);
  int r=linear(arr,n);
  cout<<r;

  return 0;
}
